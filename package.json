{
  "name": "guerra-da-live-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "simulate": "npm run build && node dist/src/main.js simulate",
    "live": "npm run build && node dist/src/main.js live",
    "logs": "npm run build && node dist/src/main.js logs",
    "arena": "npm run build && node dist/src/main.js arena",
    "arena:demo": "npm run build && node dist/src/main.js arena --demo",
    "start": "node dist/src/main.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "tiktok-live-connector": "2.4.4"
  },
  "devDependencies": {
    "@types/node": "26.5.1",
    "typescript": "7.0.2"
  }
}
